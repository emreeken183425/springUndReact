server:
  port: 8080

spring:
  datasource:
    username: root
    password: "" # E?er ?ifre bo?sa bu ?ekilde b?rakabilirsiniz
    url: jdbc:mysql://localhost:3306/springundreactstudent?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
